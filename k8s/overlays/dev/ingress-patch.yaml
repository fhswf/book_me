apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appointme-ingress
spec:
  rules:
    - host: appointme.gawron.cloud
      http:
        paths:
          - backend:
              service:
                name: client
                port:
                  number: 80
            path: /
            pathType: Prefix
          - backend:
              service:
                name: appointme
                port:
                  number: 5000
            path: /api
            pathType: Prefix
